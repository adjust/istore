istore
======

The idea of istore is basically having an integer based hstore (thus the name)
which supports operator like + and aggregates like SUM (see tests for example
usage).

Changes
-------

Version 0.1.1:

* Add containment operators @> and <@
* Add Git index opclasses to support operators ? and @>
